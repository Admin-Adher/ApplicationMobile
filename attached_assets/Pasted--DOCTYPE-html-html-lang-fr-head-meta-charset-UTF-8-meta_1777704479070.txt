<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Loader — Variante 4</title>
<style>
  :root {
    --navy:   #1B2C4A;
    --orange: #F5A623;
    --bg:     #F5F6FA;
    --card-size: 158px;
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  body {
    background: #E8EBF3;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Segoe UI', sans-serif;
  }

  /* ── Phone frame ── */
  .screen {
    width: 360px;
    height: 720px;
    background: var(--bg);
    border-radius: 40px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 44px;
    box-shadow:
      0 2px 8px rgba(27,44,74,0.06),
      0 20px 60px rgba(27,44,74,0.14),
      0 0 0 1px rgba(27,44,74,0.05);
    position: relative;
    overflow: hidden;
  }

  /* Subtle ambient light blob behind the card */
  .ambient {
    position: absolute;
    width: 280px;
    height: 280px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(245,166,35,0.07) 0%, transparent 70%);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -62%);
    animation: ambient-breathe 3s ease-in-out infinite;
    pointer-events: none;
  }
  @keyframes ambient-breathe {
    0%,100% { opacity: 0.6; transform: translate(-50%, -62%) scale(1);   }
    50%     { opacity: 1;   transform: translate(-50%, -62%) scale(1.15); }
  }

  /* ══════════════════════════════════════════
     LOGO CARD
  ══════════════════════════════════════════ */
  .logo-wrap {
    position: relative;
    width: var(--card-size);
    height: var(--card-size);
  }

  .logo-card {
    width: var(--card-size);
    height: var(--card-size);
    background: var(--navy);
    border-radius: 35px;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;          /* contient le scan line */
    box-shadow:
      0 6px 28px rgba(27,44,74,0.26),
      0 2px 6px  rgba(27,44,74,0.10);
    opacity: 0;
    animation:
      card-in       0.4s cubic-bezier(0.34,1.56,0.64,1) 0.1s forwards,
      card-float    4s   ease-in-out                     1.8s infinite;
  }

  @keyframes card-in {
    from { opacity: 0; transform: scale(0.8) translateY(12px); }
    to   { opacity: 1; transform: scale(1)   translateY(0);    }
  }

  /* Subtle 3-D float after reveal */
  @keyframes card-float {
    0%,100% { transform: translateY(0px)   rotateX(0deg)  rotateY(0deg);  }
    25%     { transform: translateY(-4px)  rotateX(1.5deg) rotateY(1deg); }
    75%     { transform: translateY(-2px)  rotateX(-1deg) rotateY(-1.5deg); }
  }

  /* Glass sheen on top half */
  .logo-card::before {
    content: '';
    position: absolute;
    inset: 0;
    border-radius: 35px;
    background: linear-gradient(150deg, rgba(255,255,255,0.09) 0%, transparent 55%);
    pointer-events: none;
    z-index: 10;
  }

  /* ── SVG ── */
  .logo-svg {
    width: 98px;
    height: 98px;
    overflow: visible;
    position: relative;
    z-index: 2;
  }

  /* Buildings revealed by clip — top to bottom (matches scan line) */
  .buildings-group {
    clip-path: inset(0 0 100% 0);  /* nothing visible at start */
    animation: reveal-ttb 1.3s cubic-bezier(0.4,0,0.15,1) 0.55s forwards;
  }
  @keyframes reveal-ttb {
    0%   { clip-path: inset(0 0 100% 0); }
    100% { clip-path: inset(0 0 0%   0); }
  }

  /* Checkmark draws after reveal */
  .checkmark-path {
    stroke-dasharray: 120;
    stroke-dashoffset: 120;
    animation: draw-ck 0.5s cubic-bezier(0.22,1,0.36,1) 2.0s forwards;
  }
  @keyframes draw-ck {
    to { stroke-dashoffset: 0; }
  }

  /* Glow burst when check finishes */
  .ck-burst {
    opacity: 0;
    animation: burst 0.45s ease-out 2.45s forwards;
  }
  @keyframes burst {
    0%   { opacity: 0.6; r: 8px;  }
    100% { opacity: 0;   r: 30px; }
  }

  /* Window idle blink */
  .win-a { animation: wblink 3.5s ease-in-out infinite 2.5s; }
  .win-b { animation: wblink 3.5s ease-in-out infinite 3.0s; }
  .win-c { animation: wblink 3.5s ease-in-out infinite 3.6s; }
  .win-d { animation: wblink 3.5s ease-in-out infinite 2.8s; }
  @keyframes wblink {
    0%,100% { opacity: 1;   }
    45%,55% { opacity: 0.2; }
  }

  /* ══════════════════════════════════════════
     SCAN LINE — the star of V4
  ══════════════════════════════════════════ */
  .scan-line {
    position: absolute;
    left: 0; right: 0;
    top: 0;
    height: 2px;
    z-index: 5;
    pointer-events: none;

    /* Glowing orange beam */
    background: linear-gradient(
      90deg,
      transparent        0%,
      rgba(245,166,35,0.3) 10%,
      #FFD580            40%,
      #F5A623            50%,
      #FFD580            60%,
      rgba(245,166,35,0.3) 90%,
      transparent        100%
    );
    box-shadow:
      0  0  6px 1px rgba(245,166,35,0.9),
      0  0 16px 3px rgba(245,166,35,0.45),
      0  0 32px 6px rgba(245,166,35,0.18);

    opacity: 0;
    animation: scan-sweep 1.3s cubic-bezier(0.4,0,0.15,1) 0.55s forwards;
  }

  @keyframes scan-sweep {
    0%   { top: 0%;   opacity: 0;   }
    4%   { opacity: 1;              }
    96%  { opacity: 1;              }
    100% { top: 100%; opacity: 0;   }
  }

  /* Afterglow trail below scan line */
  .scan-trail {
    position: absolute;
    left: 0; right: 0;
    top: 0;
    height: 0;
    z-index: 4;
    background: linear-gradient(
      180deg,
      rgba(245,166,35,0.06) 0%,
      transparent 100%
    );
    pointer-events: none;
    opacity: 0;
    animation: trail-grow 1.3s cubic-bezier(0.4,0,0.15,1) 0.55s forwards;
  }
  @keyframes trail-grow {
    0%   { height: 0%;    opacity: 0; }
    4%   { opacity: 1;               }
    96%  { opacity: 1;               }
    100% { height: 100%;  opacity: 0; }
  }

  /* Idle scan — gentle low-opacity sweep looping */
  .scan-idle {
    position: absolute;
    left: 0; right: 0;
    top: -2px;
    height: 1px;
    z-index: 5;
    pointer-events: none;
    background: linear-gradient(
      90deg,
      transparent 0%, rgba(245,166,35,0.5) 40%,
      rgba(255,213,128,0.7) 50%, rgba(245,166,35,0.5) 60%,
      transparent 100%
    );
    box-shadow: 0 0 8px 2px rgba(245,166,35,0.3);
    opacity: 0;
    animation: idle-scan 3.5s ease-in-out 2.5s infinite;
  }
  @keyframes idle-scan {
    0%         { top: 0%;   opacity: 0;    }
    5%         { opacity: 0.5;             }
    95%        { opacity: 0.5;             }
    100%       { top: 100%; opacity: 0;    }
  }

  /* Corner brackets (HUD feel) */
  .bracket {
    position: absolute;
    width: 18px; height: 18px;
    opacity: 0;
    animation: bracket-in 0.3s ease-out forwards;
    pointer-events: none;
    z-index: 8;
  }
  .bracket::before, .bracket::after {
    content: '';
    position: absolute;
    background: var(--orange);
    border-radius: 1px;
  }
  .bracket::before { width: 2px; height: 100%; }
  .bracket::after  { width: 100%; height: 2px; }

  .br-tl { top: 10px; left: 10px; animation-delay: 1.85s; }
  .br-tl::before { top: 0; left: 0; }
  .br-tl::after  { top: 0; left: 0; }

  .br-tr { top: 10px; right: 10px; animation-delay: 1.9s; }
  .br-tr::before { top: 0; right: 0; left: auto; }
  .br-tr::after  { top: 0; right: 0; left: auto; }

  .br-bl { bottom: 10px; left: 10px; animation-delay: 1.95s; }
  .br-bl::before { bottom: 0; top: auto; left: 0; }
  .br-bl::after  { bottom: 0; top: auto; left: 0; }

  .br-br { bottom: 10px; right: 10px; animation-delay: 2.0s; }
  .br-br::before { bottom: 0; top: auto; right: 0; left: auto; }
  .br-br::after  { bottom: 0; top: auto; right: 0; left: auto; }

  @keyframes bracket-in {
    from { opacity: 0; transform: scale(0.6); }
    to   { opacity: 0.55; transform: scale(1); }
  }

  /* ══════════════════════════════════════════
     TEXT
  ══════════════════════════════════════════ */
  .text-block {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 5px;
    opacity: 0;
    animation: fade-up 0.45s ease-out 0.3s forwards;
  }
  @keyframes fade-up {
    from { opacity:0; transform:translateY(8px); }
    to   { opacity:1; transform:translateY(0); }
  }
  .app-name {
    font-size: 22px;
    font-weight: 700;
    color: var(--navy);
    letter-spacing: -0.3px;
  }
  .tagline {
    font-size: 12px;
    color: rgba(27,44,74,0.38);
    letter-spacing: 0.04em;
  }

  /* ══════════════════════════════════════════
     PROGRESS BAR — identique V1
  ══════════════════════════════════════════ */
  .progress-wrap {
    width: 200px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 12px;
    opacity: 0;
    animation: fade-up 0.45s ease-out 0.55s forwards;
  }
  .progress-track {
    width: 100%;
    height: 3px;
    background: rgba(27,44,74,0.08);
    border-radius: 99px;
    overflow: hidden;
  }
  .progress-fill {
    height: 100%;
    border-radius: 99px;
    background: linear-gradient(90deg, var(--navy) 0%, var(--orange) 100%);
    animation: progress-sweep 2.4s cubic-bezier(0.4,0,0.2,1) 0.8s infinite;
    transform-origin: left center;
  }
  @keyframes progress-sweep {
    0%   { transform: scaleX(0)  translateX(0%);   opacity: 1; }
    65%  { transform: scaleX(1)  translateX(0%);   opacity: 1; }
    100% { transform: scaleX(1)  translateX(102%); opacity: 0; }
  }
  .loading-label {
    font-size: 11px;
    font-weight: 500;
    color: rgba(27,44,74,0.32);
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }
  .dot { animation: dot-blink 1.4s ease-in-out infinite; }
  .dot:nth-child(2) { animation-delay: 0.2s; }
  .dot:nth-child(3) { animation-delay: 0.4s; }
  @keyframes dot-blink {
    0%,80%,100% { opacity: 0.2; }
    40%         { opacity: 1;   }
  }

  /* ── Bottom pill ── */
  .brand-bottom {
    position: absolute;
    bottom: 30px;
    opacity: 0;
    animation: fade-up 0.4s ease-out 0.9s forwards;
  }
  .brand-pill {
    display: flex;
    align-items: center;
    gap: 6px;
    background: white;
    border: 1px solid rgba(27,44,74,0.07);
    border-radius: 99px;
    padding: 6px 14px 6px 10px;
    box-shadow: 0 2px 8px rgba(27,44,74,0.06);
  }
  .bp-dot {
    width: 7px; height: 7px;
    background: var(--orange);
    border-radius: 50%;
    animation: bp-pulse 2s ease-in-out infinite;
  }
  @keyframes bp-pulse {
    0%,100% { transform: scale(1); }
    50%     { transform: scale(1.5); }
  }
  .bp-label {
    font-size: 12px;
    font-weight: 600;
    color: var(--navy);
  }
</style>
</head>
<body>

<div class="screen">
  <div class="ambient"></div>

  <!-- ── LOGO ── -->
  <div class="logo-wrap">
    <div class="logo-card">

      <!-- Scan line (main sweep) -->
      <div class="scan-line"></div>
      <div class="scan-trail"></div>
      <!-- Idle scan (loops after reveal) -->
      <div class="scan-idle"></div>

      <!-- HUD corner brackets -->
      <div class="bracket br-tl"></div>
      <div class="bracket br-tr"></div>
      <div class="bracket br-bl"></div>
      <div class="bracket br-br"></div>

      <!-- SVG Logo -->
      <svg class="logo-svg" viewBox="0 0 100 100" fill="none">
        <defs>
          <filter id="ck-glow">
            <feGaussianBlur in="SourceGraphic" stdDeviation="1.8" result="b"/>
            <feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge>
          </filter>
          <filter id="bldg-glow">
            <feGaussianBlur in="SourceGraphic" stdDeviation="0.6" result="b"/>
            <feMerge><feMergeNode in="b"/><feMergeNode in="SourceGraphic"/></feMerge>
          </filter>
        </defs>

        <!-- ── Buildings (revealed top→bottom by clip) ── -->
        <g class="buildings-group" filter="url(#bldg-glow)">

          <!-- Sol -->
          <line x1="10" y1="86" x2="90" y2="86"
            stroke="#F5A623" stroke-width="3" stroke-linecap="round"/>

          <!-- Bâtiment gauche -->
          <polyline points="12,86 12,58 24,58 24,86"
            stroke="#F5A623" stroke-width="2.4" stroke-linejoin="round" fill="none"/>
          <rect class="win-a" x="14.5" y="63" width="4"   height="5" rx="0.5" fill="#F5A623"/>
          <rect class="win-b" x="19.5" y="63" width="2"   height="5" rx="0.5" fill="#F5A623"/>
          <rect class="win-c" x="14.5" y="71" width="4"   height="5" rx="0.5" fill="#F5A623"/>
          <rect class="win-d" x="19.5" y="71" width="2"   height="5" rx="0.5" fill="#F5A623"/>

          <!-- Bâtiment gauche-centre -->
          <polyline points="22,86 22,44 36,44 36,86"
            stroke="#F5A623" stroke-width="2.4" stroke-linejoin="round" fill="none"/>
          <rect class="win-b" x="24.5" y="49" width="4"   height="5" rx="0.5" fill="#F5A623"/>
          <rect class="win-a" x="30"   y="49" width="3"   height="5" rx="0.5" fill="#F5A623"/>
          <rect class="win-c" x="24.5" y="57" width="4"   height="5" rx="0.5" fill="#F5A623"/>
          <rect class="win-d" x="30"   y="57" width="3"   height="5" rx="0.5" fill="#F5A623"/>
          <rect class="win-a" x="24.5" y="65" width="4"   height="5" rx="0.5" fill="#F5A623"/>

          <!-- Bâtiment centre (le plus haut) -->
          <path d="M34,86 L34,38 L50,18 L66,38 L66,86"
            stroke="#F5A623" stroke-width="2.8" stroke-linejoin="round" fill="none"/>
          <rect x="38" y="68" width="24" height="18" fill="#F5A623" opacity="0.11"/>
          <rect class="win-c" x="39"   y="44" width="5"   height="7" rx="0.5" fill="#F5A623"/>
          <rect class="win-b" x="56"   y="44" width="5"   height="7" rx="0.5" fill="#F5A623"/>
          <rect class="win-d" x="39"   y="55" width="5"   height="7" rx="0.5" fill="#F5A623"/>
          <rect class="win-a" x="56"   y="55" width="5"   height="7" rx="0.5" fill="#F5A623"/>

          <!-- Bâtiment droite-centre -->
          <polyline points="64,86 64,44 78,44 78,86"
            stroke="#F5A623" stroke-width="2.4" stroke-linejoin="round" fill="none"/>
          <rect class="win-d" x="66.5" y="49" width="4"   height="5" rx="0.5" fill="#F5A623"/>
          <rect class="win-c" x="72"   y="49" width="3"   height="5" rx="0.5" fill="#F5A623"/>
          <rect class="win-b" x="66.5" y="57" width="4"   height="5" rx="0.5" fill="#F5A623"/>
          <rect class="win-a" x="72"   y="57" width="3"   height="5" rx="0.5" fill="#F5A623"/>
          <rect class="win-d" x="66.5" y="65" width="4"   height="5" rx="0.5" fill="#F5A623"/>

          <!-- Bâtiment droit -->
          <polyline points="76,86 76,58 88,58 88,86"
            stroke="#F5A623" stroke-width="2.4" stroke-linejoin="round" fill="none"/>
          <rect class="win-c" x="78.5" y="63" width="4"   height="5" rx="0.5" fill="#F5A623"/>
          <rect class="win-b" x="78.5" y="71" width="4"   height="5" rx="0.5" fill="#F5A623"/>
        </g>

        <!-- ── Burst glow au finish ── -->
        <circle class="ck-burst" cx="62" cy="37" r="0" fill="#F5A623"/>

        <!-- ── Coche ── -->
        <path class="checkmark-path"
          d="M46,42 L54,52 L76,24"
          stroke="#F5A623" stroke-width="5.5"
          stroke-linecap="round" stroke-linejoin="round"
          fill="none" filter="url(#ck-glow)"/>
      </svg>

    </div>
  </div>

  <!-- ── Texte ── -->
  <div class="text-block">
    <div class="app-name">Tropicalia</div>
    <div class="tagline">Gestion de réserves</div>
  </div>

  <!-- ── Barre V1 ── -->
  <div class="progress-wrap">
    <div class="progress-track">
      <div class="progress-fill"></div>
    </div>
    <div class="loading-label">
      Chargement<span class="dot">.</span><span class="dot">.</span><span class="dot">.</span>
    </div>
  </div>

  <!-- Brand pill -->
  <div class="brand-bottom">
    <div class="brand-pill">
      <div class="bp-dot"></div>
      <div class="bp-label">En ligne</div>
    </div>
  </div>

</div>

</body>
</html>
